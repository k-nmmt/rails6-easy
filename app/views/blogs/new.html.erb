<h1>新規投稿フォーム</h1>
  <hr>
<%= form_with(model: @blog, local: true) do |f| %>
  <div>
      <%= f.label :title, "タイトル" %><br>
    <%= f.text_field :title %>
  </div>
  <div>
     <%= f.label :body, "本文" %><br>
    <%= f.text_area :body %>
  </div>
  <%= f.submit "投稿する"%>
<% end %>